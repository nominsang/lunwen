<%= form_with(model: alternative_title) do |form| %>
  <% if alternative_title.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(alternative_title.errors.count, "error") %> prohibited this alternative_title from being saved:</h2>

      <ul>
        <% alternative_title.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :language_id, style: "display: block" %>
    <%= form.text_field :language_id %>
  </div>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
